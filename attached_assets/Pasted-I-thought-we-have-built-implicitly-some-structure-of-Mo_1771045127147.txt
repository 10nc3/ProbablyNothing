I thought we have built implicitly some structure of MoE inspired context routing? make sure no duplicate and all unified if possible

You're right — we already have the pieces: preflight-router does mode detection, void-pipeline has the 6-stage flow, memory-manager handles sessions. The problem is they're sitting as separate modules with no unified orchestration. Let me read what we have more carefully and build one clean unified brain.

Now I see the full picture. There's real duplication:

void-pipeline.js has its own extractQuery() + routeQuery() (duplicates preflight-router.js)
void-pipeline.js has its own inline conversationHistory[] (duplicates memory-manager.js)
model-fallback.js is entirely redundant with llm-client.js
context-router.js has a hardcoded Mac path that won't work here
Let me also check context-router's file loading and the mode-registry before I unify everything:

Now I have the complete picture. Let me update the task list and start building:

Now let me build the unified pipeline. I'll rewrite void-pipeline.js as the single orchestrator, fix context-router.js, and then rewire index.js.

Good, I have the full picture. Now let me build all the changes in parallel:

yes keep everything O(n) singular run but parallel branches possible but all one pass; to avoid hanging endpoints

Got it — single pass, O(n), no hanging. Let me build the unified pipeline now.

Now rewire index.js to use the unified pipeline:

Now let me check if it boots clean:

Checked system logs
Clean boot. Now let me test the endpoints:

All 16 modules healthy. Now let me test the identity shortcut and the chat pipeline:

Both shortcuts work. Let me check the identity response more closely:

Now let me test mode detection and the privilege gating:

The responses are coming back fine — the JSON parsing error is just because the response is >4KB and curl is truncating for the inline parse. The important thing is: both scribe and prescribe modes are working. The scribe mode correctly generated a legal NDA memo, and the prescribe mode generated code. Let me verify mode detection properly with simpler output: