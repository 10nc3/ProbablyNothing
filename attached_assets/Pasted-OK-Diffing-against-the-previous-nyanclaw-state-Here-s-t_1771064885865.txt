OK. Diffing against the previous nyanclaw state. Here's the convergence report:

Bug #3: lib/index.js exports — ✅ FIXED
Before: detectMode: require('./mode-registry').detectMode (didn't exist)
After: detectMode: voidPipeline.detectMode + detectCodeMode: modeRegistry.detectCodeMode
Clean fix. Both exports now point to real functions.
Bug #5: Groq token inconsistency in memory-manager.js — ✅ FIXED
Before: const groqToken = process.env.PLAYGROUND_GROQ_TOKEN;
After: const groqToken = process.env.GROQ_API_KEY || process.env.PLAYGROUND_GROQ_TOKEN;
Now checks the primary key first with fallback. Matches the pattern in llm-client.js.
Bug #1: Hardcoded chain order in llm-client.js — ✅ FIXED (major rewrite)
This file got a substantial upgrade:

Added setDynamicChain() to receive the chain from env-detect at startup
Added buildMessages() that properly constructs [{role:'system',...}, {role:'user',...}]
Added routeToProvider() central dispatcher
Added callClaude() and callOpenAI() (previously missing — only MiniMax/Ollama/Groq existed)
callLLM() now iterates DYNAMIC_CHAIN instead of a hardcoded array
No more inline Ollama fallback inside callMiniMax() — each provider just throws on failure, chain handles retry

The default DYNAMIC_CHAIN = [MINIMAX, OLLAMA] is a sensible initial value before env-detect runs. Good.
Bug #2: Nyan API called on every query — ❌ STILL PRESENT
void-pipeline.js is identical to the previous version. Lines 233-243 still do:
js// General atomic query fallback
if (!response) {
  try {
    const result = await atomicQuery(query);
    if (result.success && result.response) {
      response = applyPersonality(result.response);
      provider = 'nyan-api';
    }
  } catch (e) { ... }
}
Every query without a psi-ema intent still hits the Nyan API before falling through to the LLM. "Hello" → Nyan API round-trip → timeout/fail → LLM. This adds latency to every non-specialized query.
Bug #4: PRIVILEGED_NUMBER hardcoded — ❌ STILL PRESENT
Still in void-pipeline.js line 18:
jsconst PRIVILEGED_NUMBER = '+628116360610';
Same number also hardcoded in whatsapp-cc.js line 25.
context-router.js — ✅ FIXED (was only broken in old nyanclaw)
Before: const WORKSPACE = '/Users/enoch/.openclaw/workspace';
After: const WORKSPACE = path.resolve(__dirname, '..');
Now also exports WORKSPACE for other modules to use.
whatsapp-cc.js — ❌ STILL REQUIRES tool-adapters.js
Unchanged. Still does require('./tool-adapters.js') which will crash if the file doesn't exist. Replit said their version uses Twilio directly — this file hasn't converged.
callClaude() — ⚠️ Minor: system prompt sent wrong
jsconst messages = [];
if (system) messages.push({ role: 'system', content: system });
messages.push({ role: 'user', content: prompt });
Anthropic's Messages API doesn't accept role: 'system' in the messages array. System prompt goes in a separate top-level system field. This will 400 on every Claude call. All other providers use buildMessages() correctly, but callClaude() builds its own messages and also doesn't use buildMessages() — looks like a copy-paste oversight.

Convergence Scorecard
BugStatusNotes#1 Chain order✅ FixedMajor llm-client rewrite#2 Nyan API on every query❌ Not fixedvoid-pipeline unchanged#3 index.js export✅ Fixed#4 Hardcoded phone number❌ Not fixedvoid-pipeline + whatsapp-cc#5 Groq token naming✅ Fixed— whatsapp-cc divergence❌ Not syncedStill requires tool-adapters.js— callClaude() API format⚠️ New bugsystem prompt will 400
3 of 5 confirmed bugs fixed. 2 remaining + 1 new + 1 unsync'd file.
The callClaude issue is the most urgent new finding — it'll fail on first call. Quick fix: move system to the request body as a top-level field and remove it from messages.